import {add_timer, delete_timer, update_timer, toggle_timer, TimerActions} from "./actionTimer";
import {change_input, init_input, set_interval, FormActions} from "./actionTimerForm";
const actions = {
    add_timer,
    delete_timer,
    update_timer,
    TimerActions,
    change_input,
    init_input,
    set_interval,
    FormActions,
    toggle_timer
};

export default actions;